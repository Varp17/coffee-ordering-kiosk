# Remaining Tasks

Remaining task count: 5

1. Run the final post-change verification pass after the last store-card/mobile-action update: targeted lint, production build, and the Playwright mobile checks.
2. Test the deployed production URL to confirm the mobile/tablet homepage switch works outside localhost and the welcome-page name state persists after reloads.
3. Audit every route that was not fully checked in the rendered mobile pass: `/build`, `/menu/:id`, `/create-recipe`, `/checkout`, `/location`, `/contact`, `/profile`, `/b2b`, `/auth`, `/payment`, and `/order-confirm`.
4. Check any remaining product or recommendation sections on those pages and convert missed mobile product displays to compact two-column marketplace-style grids.
5. Do a cross-device visual pass for the footer wave/marquee and bottom-nav spacing at small phone, large phone, and tablet widths, including safe-area behavior.
defiine using //coment// using each and every section in the codebase if the section is already defined do not touch that just do for the codebase where its section are not defined.
reduce the image size of each and every image no image should exceed 1mb size
